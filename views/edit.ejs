<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit</title>
  </head>
  <body>
    <a href="/skills/<%= skill.id %>">Back</a>
    <form action="/skills/<%= skill.id %>" method="POST" autocomplete="off">
      <label for="skill">Skill</label>
      <input type="text" name="skill" value="<%= skill.skill %>" />
      <label for="type">Type</label>
      <input type="text" name="type" value="<%= skill.type %>" />
      <button type="submit">Finish Edit</button>
    </form>
  </body>
</html>
