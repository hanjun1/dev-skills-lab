<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Catalog</title>
</head>
<body>
    <% for (let item of catalog) { %>
        <!-- <li>id: <%= item.id %>, item: <%= item.product %>, size: <%= item.size %>, price: <%= item.price %>  </li> -->
        <a href="/skills/<%= item.id %>"><%= item.id %></a>
    <% } %>  
</body>
</html>